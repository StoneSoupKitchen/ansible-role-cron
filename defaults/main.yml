---
# States of the cron, anacron, and at packages. Use `present` to make sure
# the package is installed, or use `latest` to make sure the package is always
# at the latest version.
cron_package_state: present
at_package_state: present
